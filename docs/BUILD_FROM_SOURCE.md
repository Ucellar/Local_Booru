# Запуск из исходников

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m playwright install
python app.py
```

Если Playwright не нужен, этот шаг можно пропустить.
